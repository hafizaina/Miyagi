using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using NUnit.Framework;

namespace KeyValueCard
{
    [TestFixture]
    public class BaseGraphicalTest
    {
        /*BaseGraphicalMiyagiKeyCard keycard = new BaseGraphicalMiyagiKeyCard("TestCard", "3 X 3", 5, 5, 2f, 2f, 2f);

        [Test]
        public void TestSetPosition()
        {
            keycard.SetPosition(3f, 3f, 3f);
            Assert.AreEqual(keycard.GetPosition().X, 3f, "The xPos is off (SetPosition())");
            Assert.AreEqual(keycard.GetPosition().Y, 3f, "The YPos is off (SetPosition())");
            Assert.AreEqual(keycard.GetPosition().Z, 3f, "The ZPos is off (SetPosition())");
        }

        [Test]
        public void TestSetSize()
        {
            int size = 4;
            keycard.SetSize(size, size);
            Assert.AreEqual(keycard.GetWidth(), size, "The width is off");
            Assert.AreEqual(keycard.GetHeight(), size, "The height is off");
        }*/
    }

}
